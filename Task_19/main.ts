// Dinner Guests

// working with task no. 15:

let dinnerList: string[] = ["samra","wasia","ramsha"];
let count_invitations: number = dinnerList.length;

console.log(`we had invited ${count_invitations} people at the dinner`);
